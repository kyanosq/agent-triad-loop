interface:
  display_name: "Contrarian Review"
  short_description: "Targeted bug hunting with a stop rule"
  default_prompt: "Use $contrarian-review to audit this target until the consecutive-noise stop rule is reached."
